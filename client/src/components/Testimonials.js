import styled from "styled-components";

const Container = styled.div`
  background-color: var(--blue);
  color: #ffffff;
  height: 100%;
  margin: 0;
`;

const Testimonials = () => {
  return <Container id="testimonials">Testimonials go here</Container>;
};

export default Testimonials;
